import 'package:flutter/material.dart';

class TextTwistPage extends StatelessWidget {
  const TextTwistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TextTwist Game'),
      ),
    );
  }
}
